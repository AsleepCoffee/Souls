Captured by `data-pipeline/live-capture/item_logger_patch.gd` (hooks `Scenes/UI/description_box.gd` — fires on
hovering any item anywhere: inventory, equipment window, auction house, ground drops), consumed by
`build-items-data.mjs`. Keyed by `item_id`. See `observation.example.json` in this folder for the expected
shape.
